# 微信公众号配置
wechat:
  app_id: "your_app_id"
  app_secret: "your_app_secret"
  token: "your_token"

# 服务器配置
server:
  port: 8080
  mode: "debug" # debug, release

# API 访问控制配置
api:
  # IP 白名单，支持多个 IP
  ip_whitelist:
    - "127.0.0.1"
  # API Key，支持多个 Key
  api_keys:
    - "your_api_key"